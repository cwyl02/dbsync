package lib

import "testing"

func TestHello(t *testing.T) {
	// if err := parseFromJsonFile("j.json", Patch{}); err == nil {
	// 	t.Errorf("parsePatch () didn't return error as expected")
	// }
}
